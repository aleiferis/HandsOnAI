Requirements: 
    pandas
    numpy
    prophet (previously fbprophet)
    matplotlib (for ploting, could be omitted)
    fastapi (for the endpoint implementation, not fully working)
    sqlalchemy (for the postgresql connection)